// // wait for the DOM to finish loading
// $(document).ready(function() {
//   // all code to manipulate the DOM
//   // goes inside this function
// });

// declare boonlean variable activePlayer; where true is for 'X' and false is for "O"
var activePlayer;

//declare startButton function
function startButton(){
  //add event listener to .button in header
  //asks for which character firstPlayer wants to be, only takes X or O
  //if X=> activePlayer = true, if O => activePlayer = false
}

//declare positionsTaken object
//forEach (.board .row) to fill object with array of {box: numberedBox, taken: false}


//declare variable boxClicked assinging querySelectorAll('box')
//forEach every box item, create event listener (function){
//searches the positionsTaken array for index of 


//
//
